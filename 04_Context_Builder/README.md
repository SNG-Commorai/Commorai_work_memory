# 04_Context_Builder

This directory is used to assemble task-specific context from local memory layers.

- `generated_contexts/` is ignored by Git except for `.gitkeep`.
- Generated context files may contain sensitive summaries and should remain local.
