# 00_System

This directory stores system-level rules, runtime logs, and index placeholders for the local memory framework.

- `logs/` is local-only and ignored by Git except for `.gitkeep`.
- `indexes/` is reserved for generated indexes and is ignored by Git except for `.gitkeep`.
- `rules/` contains public governance and workflow rules.
