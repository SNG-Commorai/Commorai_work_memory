# Governance Rules

- When duplicate content is found, record the duplicate relationship instead of deleting it directly.
- When conflicting content is found, preserve the conflict record and wait for user confirmation.
- Expired short-term memory should be reviewed, migrated, or archived on a regular cadence.
- Run the sensitive-file scan before pushing.
- If you cannot confirm that a file is safe to publish, do not commit it.
