# Retrieval Rules

- During retrieval, prioritize combined filters by project name, field, tags, and keywords.
- When exporting context, distinguish Base Memory, Project Memory, and Short-Term Memory.
- Store generated context files only in `04_Context_Builder/generated_contexts/`.
- Generated results should stay out of Git by default.
