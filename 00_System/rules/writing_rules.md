# Writing Rules

- Write to Base Memory only when the user explicitly provides a field.
- Write to Project Memory only when the user explicitly provides a project name.
- Default uncertain ownership to Short-Term Memory.
- Return a write summary after each write, including the location, type, and suggested next steps.
- If sensitive content is detected, sanitize it first or stop the write.
